#include "main.h"

/**
 * _isupper - fuction that verifies if a character is uppercase or not
 *
 * @c: tested character
 * Retunr: 1 if it is uppercase or 0 if it is not
 */

int _isupper(int c)
{
	if((c >= 'A') && (c <= 'Z'))
		return (1);
	

		return (0);
}
