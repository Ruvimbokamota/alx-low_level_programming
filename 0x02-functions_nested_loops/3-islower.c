#include <stdio.h>

/**
 * _islower for checking for lowercase characters
 * Return: return 1 if lowercase and 0 else
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return;
	else 
		return(0);
}
